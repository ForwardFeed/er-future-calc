{
  "name": "futurecalc",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "run-ui": "cd ui && bun dev",
    "build-calc": "cd calc && wasm-pack build --dev --target web --features v2_5"
  }
}
